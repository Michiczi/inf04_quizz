{
	"name": "login-form-monorepo",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"postinstall": "npm run install",
		"install": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
		"start": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
		"build": "npm run build --prefix frontend",
		"start:prod": "npm run build && node backend/server.js"
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	},
	"engines": {
		"node": ">=18.0.0"
	}
}
